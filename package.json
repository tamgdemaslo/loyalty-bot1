{
  "name": "loyalty-bot-system",
  "version": "1.0.0",
  "description": "Telegram Loyalty Bot System with Mini App",
  "main": "miniapp/server.js",
  "scripts": {
    "start": "cd miniapp && npm start",
    "build": "cd miniapp && npm install && npm rebuild better-sqlite3",
    "postinstall": "cd miniapp && npm install && npm rebuild better-sqlite3"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "pg": "^8.16.3"
  }
}
